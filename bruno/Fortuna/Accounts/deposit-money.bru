meta {
  name: Deposit Money
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/accounts/{{accountId}}/deposit
  body: json
}

body:json {
  {
    "amount": 250.00,
    "currency": "PLN",
    "title": "Wplata testowa"
  }
}

tests {
  test("deposit returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
